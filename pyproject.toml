[project]
name = "call-me-maybe"
version = "1.0.0"
dependencies = [
    "numpy",
    "pydantic",
    "jsonschema",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"